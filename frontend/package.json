{
  "name": "check-mail",
  "version": "0.0.1",
  "description": "Chess by email.",
  "scripts": {
    "build": "gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@redsift/ui-rs-core": "^0.8.3",
    "@redsift/ui-rs-hero": "^0.4.0",
    "gulp-download": "0.0.1",
    "gulp-replace": "^0.5.4",
    "gulp-unzip": "^0.2.0"
  },
  "devDependencies": {
    "@redsift/sift-sdk-web": "0.7.0",
    "@redsift/redsift-bundler": "0.7.3",
    "gulp": "^3.9.1"
  }
}
